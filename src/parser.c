#include "parser.h"
#include "dmls.h"
#include <assert.h>

void dmls_arguments_init_with_sys(DmlsArguments *arguments, const char **textargs, int nargs)
{
	assert(arguments != null);

	if (nargs == 3) {
	}
}
